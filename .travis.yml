language: rust

rust:
  - stable
  - beta
  - nightly
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true

services:
  - docker

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master