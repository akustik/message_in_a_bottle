version: '3.1'

services:

  redis:
    build: docker/redis

  web:
    build: 
      context: .
      dockerfile: Dockerfile.debug
    restart: always
    ports:
      - 8080:$PORT
    environment:
      REDIS_URL: redis://redis:6379